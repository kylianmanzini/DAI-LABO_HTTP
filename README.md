# DAI-LABO_HTTP

## How to start
```
//build the docker image
server.sh build
//start the docker image in a new container
server.sh start
//stop the container
server.sh stop
//build then start
server.sh buildstart
```

